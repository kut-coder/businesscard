<!DOCTYPE html>
<html lang="ru">
<head>
<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>Демо — конструктор материалов специалиста</title>
<style>
  :root{
    --ink:#20231f;
    --muted:#71756d;
    --paper:#f6f3ed;
    --paper-2:#eeebe4;
    --white:#fffdf9;
    --line:#d9d5cc;
    --green:#748a72;
    --green-dark:#435944;
    --green-pale:#dfe8dd;
    --peach:#ead9c9;
    --shadow:0 24px 70px rgba(53,52,44,.11);
    --radius:28px;
  }
  *{box-sizing:border-box}
  body{margin:0;background:#ece9e2;color:var(--ink);font-family:Inter,ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}
  button,input,textarea{font:inherit}
  button{color:inherit}
  .page{max-width:1240px;margin:0 auto;padding:42px 22px 70px}
  .eyebrow{font-size:12px;letter-spacing:.18em;text-transform:uppercase;color:var(--green-dark);font-weight:700}
  h1,h2,h3,p{margin-top:0}
  .intro{display:grid;grid-template-columns:1.08fr .92fr;gap:38px;align-items:end;margin-bottom:24px}
  .intro h1{font-family:Georgia,"Times New Roman",serif;font-weight:400;line-height:1.02;font-size:clamp(40px,5.6vw,76px);letter-spacing:-.035em;margin:10px 0 18px}
  .intro h1 em{color:var(--green);font-weight:400}
  .intro p{font-size:18px;line-height:1.55;color:#555a52;max-width:540px;margin-bottom:6px}
  .mini-note{border-left:1px solid #c8c3b9;padding:6px 0 6px 22px;color:#555a52;line-height:1.5;font-size:14px}
  .app{background:var(--paper);border:1px solid rgba(255,255,255,.75);box-shadow:var(--shadow);border-radius:34px;overflow:hidden}
  .appbar{display:flex;align-items:center;justify-content:space-between;padding:17px 22px;border-bottom:1px solid var(--line);background:rgba(255,255,255,.45);gap:16px}
  .brand{display:flex;align-items:center;gap:11px;font-size:13px;font-weight:700;letter-spacing:.02em}
  .brand-mark{width:27px;height:27px;border-radius:50%;background:var(--green);position:relative}
  .brand-mark:after{content:"";position:absolute;width:10px;height:15px;border:1px solid white;border-left:0;border-bottom:0;border-radius:100% 0;left:8px;top:5px;transform:rotate(-20deg)}
  .demo-pill{font-size:11px;text-transform:uppercase;letter-spacing:.15em;background:#e5e0d6;border-radius:999px;padding:8px 12px;color:#66685f}
  .workspace{display:grid;grid-template-columns:minmax(330px,.82fr) minmax(440px,1.18fr);min-height:720px}
  .controls{padding:28px 30px 30px;border-right:1px solid var(--line);background:rgba(255,255,255,.25)}
  .preview-side{padding:28px;background:#e9e5dc;display:flex;flex-direction:column;min-width:0}
  .step{font-size:11px;text-transform:uppercase;letter-spacing:.14em;color:#8a8d84;font-weight:700;margin-bottom:9px}
  .section-title{font-family:Georgia,"Times New Roman",serif;font-size:27px;font-weight:400;line-height:1.12;margin-bottom:18px}
  .upload{display:flex;align-items:center;justify-content:space-between;gap:14px;padding:14px 15px;background:var(--white);border:1px dashed #bbb8af;border-radius:16px;margin-bottom:25px}
  .upload-meta{min-width:0}
  .upload strong{display:block;font-size:13px;margin-bottom:3px}
  .upload span{display:block;font-size:11px;color:#81837c;white-space:nowrap;overflow:hidden;text-overflow:ellipsis;max-width:230px}
  .file-input{display:none}
  .soft-btn,.export-btn,.format-btn,.theme-chip{border:0;cursor:pointer;transition:.2s ease}
  .soft-btn{background:var(--green-pale);color:var(--green-dark);border-radius:12px;padding:10px 13px;font-weight:700;font-size:12px;white-space:nowrap}
  .soft-btn:hover{transform:translateY(-1px);background:#d1dfcf}
  .formats{display:grid;grid-template-columns:repeat(3,1fr);gap:8px;margin-bottom:26px}
  .format-btn{padding:12px 8px;border-radius:13px;background:#ebe7df;border:1px solid transparent;font-size:12px;font-weight:700}
  .format-btn.active{background:var(--green-dark);color:white;box-shadow:0 6px 18px rgba(67,89,68,.18)}
  .fields{display:grid;gap:12px}
  .field label{display:flex;justify-content:space-between;font-size:11px;font-weight:700;color:#62665e;margin:0 2px 6px}
  .field label span{color:#aaa9a3;font-weight:500}
  input,textarea{width:100%;border:1px solid #d4d0c7;background:rgba(255,255,255,.72);border-radius:13px;padding:12px 13px;color:var(--ink);outline:none;transition:.2s;resize:vertical}
  input:focus,textarea:focus{border-color:#98a997;box-shadow:0 0 0 3px rgba(116,138,114,.11);background:white}
  textarea{min-height:78px;line-height:1.45}
  .compact{min-height:64px}
  .tip{display:flex;gap:10px;margin-top:19px;padding:13px 14px;background:#e8eee6;border-radius:14px;font-size:11px;line-height:1.45;color:#536054}
  .tip-dot{flex:none;width:7px;height:7px;border-radius:50%;background:var(--green);margin-top:4px}
  .preview-head{display:flex;align-items:flex-start;justify-content:space-between;gap:18px;margin-bottom:16px}
  .preview-head h3{font-family:Georgia,"Times New Roman",serif;font-size:27px;font-weight:400;margin:3px 0 0}
  .live{font-size:11px;display:flex;align-items:center;gap:7px;color:#6f736b;margin-top:7px}
  .live:before{content:"";width:7px;height:7px;border-radius:50%;background:#7ca179;box-shadow:0 0 0 4px rgba(124,161,121,.15)}
  .paper-wrap{flex:1;display:flex;align-items:center;justify-content:center;min-height:530px;position:relative}
  .paper{width:min(100%,430px);aspect-ratio:210/297;background:#faf8f3;position:relative;overflow:hidden;box-shadow:0 22px 50px rgba(44,44,39,.18);border-radius:4px;isolation:isolate}
  .bg-image{position:absolute;inset:0;width:100%;height:100%;object-fit:cover;z-index:-3;display:none}
  .paper.custom-bg .bg-image{display:block}
  .paper.custom-bg .default-bg{opacity:.16}
  .paper.custom-bg .document-layer{background:rgba(255,253,248,.78);backdrop-filter:blur(2px)}
  .default-bg{position:absolute;inset:0;z-index:-2;transition:.2s;background:linear-gradient(145deg,#f7f2e9 0 58%,#e2e9dd 58% 100%)}
  .default-bg:before{content:"";position:absolute;width:220px;height:220px;border-radius:50%;background:#d5dfce;right:-110px;top:-80px}
  .default-bg:after{content:"";position:absolute;width:230px;height:230px;border-radius:50%;border:1px solid rgba(67,89,68,.18);left:-155px;bottom:-95px}
  .leaf{position:absolute;right:27px;top:42px;width:42px;height:92px;opacity:.75;z-index:-1}
  .leaf span{position:absolute;width:22px;height:38px;background:#879b80;border-radius:100% 0 100% 0;transform-origin:bottom right}
  .leaf span:nth-child(1){right:4px;top:0;transform:rotate(18deg)}
  .leaf span:nth-child(2){left:2px;top:25px;transform:scaleX(-1) rotate(8deg)}
  .leaf span:nth-child(3){right:0;top:49px;transform:rotate(25deg)}
  .leaf:after{content:"";position:absolute;width:1px;height:88px;background:#758a70;right:18px;top:4px;transform:rotate(15deg)}
  .doc-logo{position:absolute;left:30px;top:27px;display:flex;align-items:center;gap:8px;font-size:9px;text-transform:uppercase;letter-spacing:.16em;color:#5f725d;font-weight:800}
  .doc-logo .brand-mark{width:18px;height:18px}.doc-logo .brand-mark:after{width:7px;height:10px;left:5px;top:3px}
  .document-layer{position:absolute;left:27px;right:27px;top:72px;bottom:30px;padding:24px 24px 22px;border-radius:2px;transition:.2s}
  .document-layer.recipe{background:rgba(255,253,249,.86)}
  .document-layer.guide{background:rgba(255,253,249,.78);border-top:5px solid #71886f}
  .document-layer.checklist{background:rgba(255,253,249,.9)}
  .doc-kicker{font-size:8px;text-transform:uppercase;letter-spacing:.17em;color:#6b8169;font-weight:800;margin-bottom:8px}
  .doc-title{font-family:Georgia,"Times New Roman",serif;font-size:28px;line-height:1.02;font-weight:400;letter-spacing:-.02em;margin:0 0 9px;overflow-wrap:anywhere}
  .doc-subtitle{font-size:10px;line-height:1.45;color:#60645d;margin-bottom:19px;white-space:pre-line}
  .rule{height:1px;background:#cfd5cb;margin:0 0 15px}
  .two-col{display:grid;grid-template-columns:.86fr 1.14fr;gap:18px}
  .doc-section-title{font-size:8px;text-transform:uppercase;letter-spacing:.13em;font-weight:800;color:#667463;margin-bottom:8px}
  .doc-text{font-size:9px;line-height:1.55;white-space:pre-line;color:#343932}
  .ingredients{margin:0;padding-left:13px;font-size:9px;line-height:1.55}
  .ingredients li{padding-left:1px}
  .recipe-note{margin-top:18px;background:#dfe8dd;border-radius:10px;padding:11px 12px;font-family:Georgia,"Times New Roman",serif;font-style:italic;font-size:11px;line-height:1.4;color:#425342}
  .guide-blocks{display:grid;gap:10px}
  .guide-card{padding:12px 13px;border:1px solid #d8d7d0;background:rgba(255,255,255,.52);border-radius:10px}
  .guide-number{font-size:8px;color:#788a75;font-weight:800;margin-bottom:4px}.guide-card strong{font-family:Georgia,"Times New Roman",serif;font-size:12px;font-weight:400}.guide-card p{font-size:9px;line-height:1.45;margin:5px 0 0;color:#555a52;white-space:pre-line}
  .checklist-items{display:grid;gap:9px;margin-top:5px}.check-item{display:grid;grid-template-columns:18px 1fr;gap:9px;align-items:flex-start;font-size:10px;line-height:1.45}.check{width:17px;height:17px;border:1px solid #82947f;border-radius:4px;position:relative}.check:after{content:"";position:absolute;width:7px;height:4px;border-left:1.5px solid #60755e;border-bottom:1.5px solid #60755e;transform:rotate(-45deg);left:4px;top:4px}
  .doc-footer{position:absolute;left:24px;right:24px;bottom:16px;display:flex;justify-content:space-between;align-items:center;color:#7b7d76;font-size:7px}.doc-footer strong{color:#526551;letter-spacing:.06em}
  .export-row{display:flex;gap:9px;justify-content:flex-end;margin-top:16px}
  .export-btn{padding:11px 15px;border-radius:12px;font-size:12px;font-weight:800;border:1px solid #c6c2b9;background:rgba(255,255,255,.55)}
  .export-btn.primary{background:var(--green-dark);color:#fff;border-color:var(--green-dark)}
  .export-btn:hover{transform:translateY(-1px)}
  .caption{font-size:11px;color:#7d7e77;text-align:right;margin-top:8px}
  .toast{position:fixed;left:50%;bottom:24px;transform:translate(-50%,15px);opacity:0;pointer-events:none;background:#272c27;color:white;padding:11px 16px;border-radius:999px;font-size:12px;transition:.25s;z-index:20;box-shadow:0 12px 30px rgba(0,0,0,.2)}
  .toast.show{opacity:1;transform:translate(-50%,0)}
  .under{display:grid;grid-template-columns:1fr 1fr;gap:14px;margin-top:18px}
  .under-card{background:#f4f0e8;border-radius:20px;padding:20px 22px;border:1px solid rgba(255,255,255,.6)}
  .under-card .num{font-size:10px;color:#8c8b84;letter-spacing:.14em;text-transform:uppercase;margin-bottom:8px}.under-card strong{display:block;font-family:Georgia,"Times New Roman",serif;font-weight:400;font-size:20px;margin-bottom:6px}.under-card p{font-size:12px;line-height:1.5;color:#666861;margin:0}
  @media(max-width:900px){.intro{grid-template-columns:1fr}.workspace{grid-template-columns:1fr}.controls{border-right:0;border-bottom:1px solid var(--line)}.preview-side{padding:22px}.paper-wrap{min-height:590px}.under{grid-template-columns:1fr}}
  @media(max-width:560px){.page{padding:28px 10px 50px}.app{border-radius:24px}.appbar{padding:14px 15px}.controls{padding:22px 18px}.preview-side{padding:20px 12px}.formats{gap:5px}.format-btn{font-size:11px;padding:11px 5px}.upload{align-items:flex-start;flex-direction:column}.paper{width:100%}.preview-head{padding:0 5px}.export-row{justify-content:stretch}.export-btn{flex:1}.intro h1{font-size:43px}}
  @media print{
    body{background:white}.page>*:not(.app){display:none}.appbar,.controls,.preview-head,.export-row,.caption{display:none!important}.page{padding:0;max-width:none}.app{box-shadow:none;border:0;border-radius:0}.workspace{display:block;min-height:0}.preview-side{padding:0;background:white}.paper-wrap{min-height:0}.paper{width:210mm;height:297mm;box-shadow:none;margin:0;border-radius:0}.document-layer{top:32mm;left:14mm;right:14mm;bottom:15mm}.doc-logo{left:15mm;top:13mm}}
</style>
</head>
<body>
<div class="page">
  <section class="intro">
    <div>
      <div class="eyebrow">06 · стиль и материалы</div>
      <h1>Ваши материалы — <em>без Canva каждый раз</em></h1>
      <p>Загрузите фирменный фон, выберите формат и напишите текст. Материал собирается сам — уже в вашей подаче.</p>
    </div>
    <div class="mini-note">Это демо будущего инструмента. В рабочей версии можно хранить несколько фирменных шаблонов, свои поля и готовые пресеты под практику специалиста.</div>
  </section>

  <section class="app">
    <div class="appbar">
      <div class="brand"><span class="brand-mark"></span> material studio</div>
      <div class="demo-pill">живое демо</div>
    </div>

    <div class="workspace">
      <div class="controls">
        <div class="step">шаг 1</div>
        <div class="section-title">Ваш фирменный фон</div>
        <div class="upload">
          <div class="upload-meta">
            <strong id="fileTitle">Демо-фон специалиста</strong>
            <span id="fileHint">цвет · знак · декоративные элементы</span>
          </div>
          <label class="soft-btn" for="bgUpload">Загрузить свой</label>
          <input class="file-input" id="bgUpload" type="file" accept="image/*" />
        </div>

        <div class="step">шаг 2</div>
        <div class="section-title">Что создаём?</div>
        <div class="formats">
          <button class="format-btn active" data-type="recipe">Рецепт</button>
          <button class="format-btn" data-type="guide">Гайд</button>
          <button class="format-btn" data-type="checklist">Чек-лист</button>
        </div>

        <div class="step">шаг 3</div>
        <div class="section-title">Добавьте свой текст</div>
        <div id="fields" class="fields"></div>
        <div class="tip"><span class="tip-dot"></span><div>Пишите прямо здесь — превью справа меняется сразу. В реальном инструменте структура и поля настраиваются под ваш способ работы.</div></div>
      </div>

      <div class="preview-side">
        <div class="preview-head">
          <div><div class="step">шаг 4</div><h3>Готовый материал</h3></div>
          <div class="live">обновляется автоматически</div>
        </div>
        <div class="paper-wrap">
          <div id="paper" class="paper">
            <img id="bgImage" class="bg-image" alt="Фирменный фон" />
            <div class="default-bg"></div>
            <div class="leaf"><span></span><span></span><span></span></div>
            <div class="doc-logo"><span class="brand-mark"></span> Мария Светлова · нутрициолог</div>
            <div id="documentLayer" class="document-layer recipe"></div>
          </div>
        </div>
        <div class="export-row">
          <button id="jpgBtn" class="export-btn">Скачать JPG</button>
          <button id="pdfBtn" class="export-btn primary">Сохранить PDF</button>
        </div>
        <div class="caption">В демо JPG скачивается сразу; PDF открывает системное окно печати → «Сохранить как PDF».</div>
      </div>
    </div>
  </section>

  <div class="under">
    <div class="under-card"><div class="num">один раз</div><strong>Настраиваем вашу систему</strong><p>Фон, логотип, размеры, поля и правила верстки задаются под ваши реальные материалы.</p></div>
    <div class="under-card"><div class="num">дальше</div><strong>Вы работаете с содержанием</strong><p>Рецепт, гайд или чек-лист собирается за минуты — без ручной верстки каждой новой страницы.</p></div>
  </div>
</div>
<div id="toast" class="toast"></div>

<script>
const DATA = {
 recipe: {
   fields:[
     ['title','Название рецепта','Тёплая тарелка с киноа'],
     ['subtitle','Короткое описание','Сытный обед, который удобно собрать заранее'],
     ['ingredients','Ингредиенты','киноа — 70 г\nтыква — 180 г\nнут — 100 г\nшпинат — горсть\nтахини — 1 ст. л.'],
     ['steps','Как приготовить','Запеките тыкву до мягкости. Сварите киноа. Соберите всё в тарелке, добавьте нут и зелень. Полейте соусом из тахини.'],
     ['note','Комментарий специалиста','Если хочется больше сытости — добавьте яйцо или порцию рыбы.']
   ]
 },
 guide: {
   fields:[
     ['title','Название гайда','Еда без гонки'],
     ['subtitle','Подзаголовок','Три опоры, которые помогают сделать питание устойчивее'],
     ['block1','Блок 1','Начните с регулярности\nНе ищите идеальный рацион. Сначала верните предсказуемые приёмы пищи.'],
     ['block2','Блок 2','Собирайте тарелку\nДобавляйте источник белка, овощи, сложные углеводы и жиры.'],
     ['block3','Блок 3','Оставляйте место жизни\nПитание должно выдерживать работу, поездки и семейные ужины.']
   ]
 },
 checklist: {
   fields:[
     ['title','Название чек-листа','Закупка на 3 дня'],
     ['subtitle','Короткая подсказка','Сохраните в телефон и отметьте то, что уже есть дома'],
     ['items','Пункты — каждый с новой строки','яйца\nгреческий йогурт\nкурица или тофу\nогурцы и томаты\nлистья салата\nгречка или киноа\nорехи\nягоды или фрукты'],
     ['note','Нижняя заметка','Не нашли конкретный продукт? Выбирайте замену из той же группы — идеальное совпадение не требуется.']
   ]
 }
};
let currentType='recipe';
const state={};
Object.keys(DATA).forEach(type=>{state[type]={};DATA[type].fields.forEach(([k,l,v])=>state[type][k]=v)});
const fieldsEl=document.getElementById('fields');
const layer=document.getElementById('documentLayer');
const paper=document.getElementById('paper');
const bgInput=document.getElementById('bgUpload');
const bgImage=document.getElementById('bgImage');

function escapeHtml(str=''){return str.replace(/[&<>'"]/g,s=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#039;','"':'&quot;'}[s]))}
function buildFields(){
  fieldsEl.innerHTML='';
  DATA[currentType].fields.forEach(([key,label])=>{
    const wrap=document.createElement('div');wrap.className='field';
    const val=state[currentType][key];
    const long=['ingredients','steps','note','block1','block2','block3','items'].includes(key);
    wrap.innerHTML=`<label>${label}<span>${val.length} зн.</span>${long?`<textarea data-key="${key}" class="${key==='note'?'compact':''}">${escapeHtml(val)}</textarea>`:`<input data-key="${key}" value="${escapeHtml(val)}">`}`;
    fieldsEl.appendChild(wrap);
  });
  fieldsEl.querySelectorAll('input,textarea').forEach(el=>el.addEventListener('input',e=>{
    state[currentType][e.target.dataset.key]=e.target.value;
    e.target.previousElementSibling.querySelector('span').textContent=e.target.value.length+' зн.';
    renderPreview();
  }));
}
function renderPreview(){
  const s=state[currentType];
  layer.className='document-layer '+currentType;
  if(currentType==='recipe'){
    const items=s.ingredients.split(/\n+/).filter(Boolean).map(x=>`<li>${escapeHtml(x)}</li>`).join('');
    layer.innerHTML=`<div class="doc-kicker">карточка рецепта</div><h2 class="doc-title">${escapeHtml(s.title)}</h2><div class="doc-subtitle">${escapeHtml(s.subtitle)}</div><div class="rule"></div><div class="two-col"><div><div class="doc-section-title">ингредиенты</div><ul class="ingredients">${items}</ul></div><div><div class="doc-section-title">приготовление</div><div class="doc-text">${escapeHtml(s.steps)}</div></div></div><div class="recipe-note">${escapeHtml(s.note)}</div><div class="doc-footer"><span>бережное питание · без жёстких правил</span><strong>@maria.health</strong></div>`;
  } else if(currentType==='guide'){
    const cards=['block1','block2','block3'].map((k,i)=>{let [head,...rest]=s[k].split('\n');return `<div class="guide-card"><div class="guide-number">0${i+1}</div><strong>${escapeHtml(head)}</strong><p>${escapeHtml(rest.join('\n'))}</p></div>`}).join('');
    layer.innerHTML=`<div class="doc-kicker">мини-гайд</div><h2 class="doc-title">${escapeHtml(s.title)}</h2><div class="doc-subtitle">${escapeHtml(s.subtitle)}</div><div class="guide-blocks">${cards}</div><div class="doc-footer"><span>сохраните, чтобы вернуться позже</span><strong>@maria.health</strong></div>`;
  } else {
    const items=s.items.split(/\n+/).filter(Boolean).map(x=>`<div class="check-item"><span class="check"></span><span>${escapeHtml(x)}</span></div>`).join('');
    layer.innerHTML=`<div class="doc-kicker">чек-лист</div><h2 class="doc-title">${escapeHtml(s.title)}</h2><div class="doc-subtitle">${escapeHtml(s.subtitle)}</div><div class="rule"></div><div class="checklist-items">${items}</div><div class="recipe-note">${escapeHtml(s.note)}</div><div class="doc-footer"><span>простые решения для обычной недели</span><strong>@maria.health</strong></div>`;
  }
}

document.querySelectorAll('.format-btn').forEach(btn=>btn.addEventListener('click',()=>{
  currentType=btn.dataset.type;
  document.querySelectorAll('.format-btn').forEach(x=>x.classList.toggle('active',x===btn));
  buildFields();renderPreview();
}));

bgInput.addEventListener('change',e=>{
 const file=e.target.files[0];if(!file)return;
 const reader=new FileReader();
 reader.onload=ev=>{bgImage.src=ev.target.result;paper.classList.add('custom-bg');document.getElementById('fileTitle').textContent=file.name;document.getElementById('fileHint').textContent='ваш фон используется в превью';showToast('Фон загружен')};
 reader.readAsDataURL(file);
});

function showToast(msg){const t=document.getElementById('toast');t.textContent=msg;t.classList.add('show');clearTimeout(window.toastTimer);window.toastTimer=setTimeout(()=>t.classList.remove('show'),1900)}

document.getElementById('pdfBtn').addEventListener('click',()=>{showToast('Откроется печать — выберите «Сохранить как PDF»');setTimeout(()=>window.print(),300)});

// Lightweight JPG export: recreate the current preview on canvas (keeps the demo dependency-free).
document.getElementById('jpgBtn').addEventListener('click',async()=>{
  const W=1240,H=1754,scale=W/430;
  const c=document.createElement('canvas');c.width=W;c.height=H;const ctx=c.getContext('2d');
  ctx.fillStyle='#f7f2e9';ctx.fillRect(0,0,W,H);
  if(bgImage.src){try{await drawCover(ctx,bgImage,0,0,W,H);ctx.fillStyle='rgba(255,253,248,.79)';ctx.fillRect(78,425,W-156,H-510)}catch(e){}}
  else{ctx.fillStyle='#dfe8dd';ctx.beginPath();ctx.arc(W+60,-40,330,0,Math.PI*2);ctx.fill();ctx.fillStyle='#f7f2e9';ctx.fillRect(78,425,W-156,H-510)}
  ctx.fillStyle='#587057';ctx.font='700 24px Arial';ctx.fillText('MARIA HEALTH · НУТРИЦИОЛОГ',86,100);
  const s=state[currentType];const left=148;let y=500;
  ctx.fillStyle='#637861';ctx.font='700 20px Arial';ctx.fillText(currentType==='recipe'?'КАРТОЧКА РЕЦЕПТА':currentType==='guide'?'МИНИ-ГАЙД':'ЧЕК-ЛИСТ',left,y);y+=58;
  ctx.fillStyle='#20231f';ctx.font='54px Georgia';y=wrap(ctx,s.title,left,y,W-296,62);y+=18;
  ctx.fillStyle='#60645d';ctx.font='28px Arial';y=wrap(ctx,s.subtitle,left,y,W-296,40);y+=35;
  ctx.strokeStyle='#cfd5cb';ctx.beginPath();ctx.moveTo(left,y);ctx.lineTo(W-left,y);ctx.stroke();y+=50;
  ctx.fillStyle='#343932';ctx.font='25px Arial';
  if(currentType==='recipe'){
    ctx.font='700 20px Arial';ctx.fillStyle='#667463';ctx.fillText('ИНГРЕДИЕНТЫ',left,y);y+=40;ctx.font='25px Arial';ctx.fillStyle='#343932';
    for(const item of s.ingredients.split(/\n+/).filter(Boolean)){y=wrap(ctx,'• '+item,left,y,W-296,36)+7}y+=20;
    ctx.font='700 20px Arial';ctx.fillStyle='#667463';ctx.fillText('ПРИГОТОВЛЕНИЕ',left,y);y+=42;ctx.font='25px Arial';ctx.fillStyle='#343932';y=wrap(ctx,s.steps,left,y,W-296,36);y+=50;ctx.fillStyle='#4b604a';ctx.font='italic 27px Georgia';wrap(ctx,s.note,left,y,W-296,39);
  }else if(currentType==='guide'){
    for(let i=1;i<=3;i++){ctx.fillStyle='#667463';ctx.font='700 19px Arial';ctx.fillText('0'+i,left,y);y+=34;let [head,...rest]=s['block'+i].split('\n');ctx.fillStyle='#20231f';ctx.font='31px Georgia';y=wrap(ctx,head,left,y,W-296,38);ctx.fillStyle='#555a52';ctx.font='24px Arial';y=wrap(ctx,rest.join(' '),left,y,W-296,34)+35}
  }else{
    for(const item of s.items.split(/\n+/).filter(Boolean)){ctx.strokeStyle='#71886f';ctx.strokeRect(left,y-20,24,24);ctx.fillStyle='#343932';ctx.font='25px Arial';y=wrap(ctx,item,left+48,y,W-344,35)+15}y+=20;ctx.fillStyle='#4b604a';ctx.font='italic 27px Georgia';wrap(ctx,s.note,left,y,W-296,39);
  }
  const a=document.createElement('a');a.href=c.toDataURL('image/jpeg',.92);a.download=`material-${currentType}.jpg`;a.click();showToast('JPG скачан');
});
function wrap(ctx,text,x,y,maxWidth,lineHeight){const words=(text||'').split(/\s+/),lines=[];let line='';for(const w of words){const test=line?line+' '+w:w;if(ctx.measureText(test).width>maxWidth&&line){lines.push(line);line=w}else line=test}if(line)lines.push(line);for(const l of lines){ctx.fillText(l,x,y);y+=lineHeight}return y}
function drawCover(ctx,img,x,y,w,h){return new Promise((res,rej)=>{const go=()=>{try{const ir=img.naturalWidth/img.naturalHeight,br=w/h;let sx=0,sy=0,sw=img.naturalWidth,sh=img.naturalHeight;if(ir>br){sw=img.naturalHeight*br;sx=(img.naturalWidth-sw)/2}else{sh=img.naturalWidth/br;sy=(img.naturalHeight-sh)/2}ctx.drawImage(img,sx,sy,sw,sh,x,y,w,h);res()}catch(e){rej(e)}};if(img.complete)go();else{img.onload=go;img.onerror=rej}})}

buildFields();renderPreview();
</script>
</body>
</html>
